FROM oven/bun:1-alpine

WORKDIR /app

# Copy package files
COPY servers/signaling-server/package.json servers/signaling-server/package-lock.json* ./

# Install dependencies
RUN bun install --frozen-lockfile

# Copy source code
COPY servers/signaling-server/ .

# Build TypeScript
RUN bun run build

EXPOSE 8080

ENV SIGNALING_PORT=8080

CMD ["bun", "run", "start"]
